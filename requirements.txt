# ═══════════════════════════════════════════════════════════════
# Koib-V-4.1 — Зависимости
# ═══════════════════════════════════════════════════════════════
# Установка: pip install -r requirements.txt
# Для Docling:  pip install docling  (опционально, отдельно)
# Для OpenAI:   pip install openai   (опционально)
# ═══════════════════════════════════════════════════════════════

# ── Парсинг документов ──────────────────────────────────────────
pymupdf>=1.23.0              # PDF: извлечение текста, таблиц, изображений
python-docx>=0.8.11          # DOCX: парсинг с сохранением структуры
Pillow>=9.0.0                # Обработка изображений
numpy>=1.24.0                # Работа с массивами (OCR)

# ── OCR (опционально) ──────────────────────────────────────────
pytesseract>=0.3.10          # Tesseract OCR
# easyocr>=1.7.0             # EasyOCR (установить отдельно при необходимости)

# ── Чанкинг ────────────────────────────────────────────────────
langchain-core>=0.1.0,<0.4.0
langchain-text-splitters>=0.0.1,<0.4.0

# ── Эмбеддинги ─────────────────────────────────────────────────
langchain-huggingface>=0.0.1,<0.4.0
sentence-transformers>=2.2.0
torch>=2.0.0

# ── Векторный индекс ───────────────────────────────────────────
langchain-community>=0.0.10,<0.4.0
faiss-cpu>=1.7.4,<2.0.0

# ── BM25 (sparse retrieval) ────────────────────────────────────
rank-bm25>=0.2.2

# ── Переранжирование ──────────────────────────────────────────
# sentence-transformers уже установлен выше (CrossEncoder)

# ── Прогресс и утилиты ────────────────────────────────────────
tqdm>=4.65.0
requests>=2.28.0

# ── Опционально: OpenAI ───────────────────────────────────────
# openai>=1.0.0
# langchain-openai>=0.0.1

# ── Опционально: Docling (лучший парсер) ──────────────────────
# docling>=1.0.0

# ── Опционально: RAGAS / DeepEval для оценки ──────────────────
# ragas>=0.1.0
# deepeval>=0.20.0
